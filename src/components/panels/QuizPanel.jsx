import { DragPanel } from "../DragPanel.jsx";

export default function QuizPanel({quizState,setQuizState,closeQuiz,startQuiz,pn,bF,bT}){
  if(!quizState)return null;
  return <DragPanel style={Object.assign({},pn,{bottom:60,left:"50%",transform:"translateX(-50%)",width:320,maxWidth:"calc(100vw - 20px)",padding:"14px 16px",zIndex:30})}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:10}}><span style={{fontSize:12,fontWeight:"bold",color:"rgba(255,220,80,0.95)"}}>🎯 天体クイズ ({quizState.idx+1}/{quizState.questions.length})</span><button style={Object.assign({},bF,{padding:"2px 6px",fontSize:9})} onClick={closeQuiz}>✕</button></div>{quizState.idx<quizState.questions.length?(function(){var q=quizState.questions[quizState.idx];return <div><div style={{fontSize:11,color:"rgba(255,255,255,0.92)",lineHeight:"17px",marginBottom:10}}>{q.q}</div><div style={{display:"flex",flexDirection:"column",gap:5}}>{q.a.map(function(ans,ai){var isAnswered=quizState.answered!==null;var isCorrect=ai===q.c;var isChosen=ai===quizState.answered;var btnStyle=Object.assign({},bF,{textAlign:"left",padding:"6px 10px",fontSize:10,background:isAnswered?(isCorrect?"rgba(80,200,100,0.35)":isChosen?"rgba(255,80,80,0.3)":"rgba(255,255,255,0.04)"):"rgba(255,255,255,0.06)",border:isAnswered?(isCorrect?"1px solid rgba(80,200,100,0.6)":isChosen?"1px solid rgba(255,80,80,0.5)":"1px solid rgba(255,255,255,0.08)"):"1px solid rgba(255,255,255,0.15)",cursor:isAnswered?"default":"pointer"});return <button key={ai} style={btnStyle} onClick={function(){if(quizState.answered!==null)return;setQuizState(function(p){return Object.assign({},p,{answered:ai,score:ai===q.c?p.score+1:p.score});});}}>{ans}</button>;})}</div>{quizState.answered!==null&&<><div style={{marginTop:8,fontSize:9,color:"rgba(150,220,180,0.85)",lineHeight:"14px"}}>💡 {q.hint}</div><button style={Object.assign({},bT("100,180,255"),{marginTop:8,width:"100%",fontSize:11,padding:"6px"})} onClick={function(){setQuizState(function(p){var next=p.idx+1;return Object.assign({},p,{idx:next,answered:null});});}}>次の問題 →</button></>}</div>;}()):<div style={{textAlign:"center"}}><div style={{fontSize:22,marginBottom:8}}>{"⭐".repeat(quizState.score)+"☆".repeat(quizState.questions.length-quizState.score)}</div><div style={{fontSize:14,color:"rgba(255,220,80,1)",fontWeight:"bold",marginBottom:6}}>{quizState.score}/{quizState.questions.length} 正解</div><div style={{fontSize:10,color:"rgba(255,255,255,0.6)",marginBottom:12}}>{quizState.score===quizState.questions.length?"完璧！地学マスター🏆":quizState.score>=3?"よくできました！⭐":"もう一度チャレンジ！"}</div><div style={{display:"flex",gap:6}}><button style={Object.assign({},bT("100,180,255"),{flex:1,fontSize:11,padding:"7px"})} onClick={startQuiz}>もう一度</button><button style={Object.assign({},bF,{flex:1,fontSize:11,padding:"7px"})} onClick={closeQuiz}>閉じる</button></div></div>}</DragPanel>;
}
